# Copie para apps/backend/.env (não commite .env).
# O Nest carrega este arquivo mesmo quando você roda `npm run dev` na raiz do monorepo.
DATABASE_URL="postgresql://USUARIO:SENHA@HOST:PORTA/NOME_DO_BANCO?schema=public&sslmode=disable"
JWT_SECRET="obrigatorio-senao-o-login-falha-secretOrPrivateKey"
PORT=3001
